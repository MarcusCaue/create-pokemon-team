import styled from "styled-components"

export const DataPokemon = styled.section`
  font-size: 1.1rem;
  
  ul {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    list-style: none;
  }
`